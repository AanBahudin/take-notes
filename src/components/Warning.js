
const Warning = ({warningMessage}) => {
    return (
        <message>
            <h1 className="font-lexend text-2xl  lowercase text-center mt-[10%] text-secondary">{warningMessage}</h1>
        </message>
    )
} 

export default Warning